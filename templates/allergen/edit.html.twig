{% extends 'base.html.twig' %}

{% block title %}Allergène{% endblock %}

{% block body %}
    <h1 class="text-center m-3">Edition d'un Allergène</h1>
    <div>
        {{ include('allergen/_form.html.twig', {'button_label': 'Sauvegarder'}) }}

        <div class="d-flex">
            <a class="btn btn-danger btn-rounded btn-sm my-0 mr-3" href="{{ path('allergen_index') }}">Revenir à la liste</a>

            {{ include('allergen/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}

{% block javascripts %}
    <script>
        var allergenName = document.getElementById('allergen_name');
        var hiddenInput = document.getElementById('allergen_image_alt');
        $('#allergen_name').on('blur', function(){
            hiddenInput.value = allergenName.value;
            console.log(hiddenInput.value);
        });
    </script>
{% endblock %}

